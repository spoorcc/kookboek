name: Deploy GitHub Pages

on:
  workflow_run:
    workflows: [Build PDF]
    types: [completed]
    branches: [main]

permissions:
  contents: write
  actions: read

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.event.workflow_run.conclusion == 'success'

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Download PDF artifact
        uses: actions/download-artifact@v4
        with:
          name: kookboek
          run-id: ${{ github.event.workflow_run.id }}
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Download register index artifact
        uses: actions/download-artifact@v4
        with:
          name: register-idx
          run-id: ${{ github.event.workflow_run.id }}
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install pypdf
        run: pip3 install pypdf

      - name: Generate recipe index
        run: python3 scripts/extract_index.py

      - name: Install Ghostscript
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends ghostscript

      - name: Build low-res web PDF
        run: python3 scripts/make_web_pdf.py KookboekFamilieSpoor.pdf docs/KookboekFamilieSpoor-web.pdf

      - name: Copy print PDF into docs
        run: cp KookboekFamilieSpoor.pdf docs/

      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./docs
